name: Update Papers Daily

on:
  # 定时任务：北京时间每天凌晨2点 (UTC 18:00)
  schedule:
    - cron: '0 18 * * *'  # UTC时间18:00 = 北京时间次日02:00
  
  # 支持手动触发
  workflow_dispatch:
    inputs:
      page_size:
        description: '每页论文数量'
        required: false
        default: '30'
      sort_by:
        description: '排序方式'
        required: false
        default: 'Hot'

# 设置权限
permissions:
  contents: write

jobs:
  update-papers:
    runs-on: ubuntu-latest
    
    steps:
      # 1. 检出代码
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      # 2. 设置 Python 环境
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      # 3. 安装依赖
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests
      
      # 4. 运行爬虫脚本
      - name: Fetch papers data
        run: |
          # 将手动输入的参数传递给脚本（如果您的脚本支持）
          python fetch_papers.py --page-size "${{ github.event.inputs.page_size }}" --sort_by "${{ github.event.inputs.sort_by }}"
          
          # 调试：检查文件是否生成
          echo "--- 检查生成的文件 ---"
          ls -l day_paper_info.js
        env:
          TZ: 'Asia/Shanghai'
      
      # 5. 检查是否有变化
      # - name: Check for changes
      #   id: git-check
      #   run: |
      #     if [ -n "$(git status --porcelain day_paper_info.js)" ]; then
      #       echo "changed=true" >> $GITHUB_OUTPUT
      #       echo "检测到文件有变化。"
      #     else
      #       echo "changed=false" >> $GITHUB_OUTPUT
      #       echo "文件无变化。"
      #     fi
      - name: Check for changes
        id: git-check
        run: |
          echo "changed=true" >> $GITHUB_OUTPUT
          echo "强制标记为有变化，确保执行上传。"

      # 6. 提交更改
      # - name: Commit and push if changed
      #   if: steps.git-check.outputs.changed == 'true'
      #   run: |
      #     git config --local user.email "github-actions[bot]@users.noreply.github.com"
      #     git config --local user.name "github-actions[bot]"
      #     git add day_paper_info.js
      #     git commit -m "🤖 Auto update papers - $(TZ=Asia/Shanghai date +'%Y-%m-%d %H:%M:%S')"
      #     # 【关键修改】明确推送到 main 分支
      #     git push origin HEAD:main
      - name: Commit and push
        if: steps.git-check.outputs.changed == 'true' 
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add day_paper_info.js
          # 关键修改：如果 git commit 因为没有改动而失败，则执行 echo 命令，让整个步骤成功
          git commit -m "Auto-update daily paper info" || echo "No changes to commit."
          git push
      
      # 7. 上传为 artifact（用于调试，不影响仓库代码）
      - name: Upload papers data as artifact
        uses: actions/upload-artifact@v4
        with:
          name: papers-data-${{ github.run_number }}
          path: day_paper_info.js
          retention-days: 7
